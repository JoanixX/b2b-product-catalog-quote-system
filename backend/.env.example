# Fictitious placeholders only. Store real values privately.
DATABASE_URL=postgresql://example_user:example_password@localhost:5432/example_database
JWT_SECRET=REPLACE_WITH_A_RANDOM_PRIVATE_SECRET
PORT=3000
CORS_ORIGIN=http://localhost:3005
RUST_LOG=info
# One-time bootstrap; remove these after creating the first operator.
BOOTSTRAP_ADMIN_EMAIL=operator@example.invalid
BOOTSTRAP_ADMIN_PASSWORD=REPLACE_WITH_A_UNIQUE_PRIVATE_PASSWORD
# Email and uploads are disabled in the generic pilot.
ENABLE_LEGACY_PUBLIC_API=false
